{% extends "base.html.twig" %}
{% block title %} lushidev blog{% endblock %}
	{% block body %}
		<div class="container">
			<div class="row">
				{% for post in posts %}
					<h4 class="text-success">{{post}}</h4>
				{% endfor %}
			</div>
		</div>
	{% endblock %}