{% extends 'base.html.twig' %}

{% block title %}creer un article {% endblock %}

	{% block body %}
		<div class="container">
			<h3 class="text-center text-capitalize mt-2">créer un poste</h3>
				<div class="row justify-content-center mt-3">
					
					{{ form_start(form) }}
					{{ form_widget(form) }}
					<button type="submit" class="btn btn-success">poster</button>
					{{ form_end(form) }}
				</div>

		</div>
	{% endblock %}